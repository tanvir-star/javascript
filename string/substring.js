let originalString = "Hello, world!";
let substring1 = originalString.substr(2, 5); // Extracts "Hello"
let substring2 = originalString.substr(7);    // Extracts "world!"

console.log(substring1); 
console.log(substring2);
